FROM ghcr.io/typst/typst:0.14.2

# Create directories for custom fonts and packages
RUN mkdir -p /usr/share/fonts/custom \
    && mkdir -p /root/.local/share/typst/packages

WORKDIR /workspace

# Keep container running
CMD ["tail", "-f", "/dev/null"]
