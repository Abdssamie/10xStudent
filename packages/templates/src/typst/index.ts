/**
 * @id: templates-index
 * @priority: high
 * @progress: 0
 * @directive: Define all 5 Typst templates with complete structure and sample content
 * @context: specs/03-typst-document-engine-templates.md#template-system
 * @checklist: [
 *   "Define TypstTemplate interface (id, name, description, category, content)",
 *   "Implement research-paper template with abstract, sections, references",
 *   "Implement report template with executive summary, findings, recommendations",
 *   "Implement essay template with introduction, body, conclusion",
 *   "Implement article template with headline, byline, lead, body",
 *   "Implement notes template with simple headings and lists",
 *   "Use #lorem() for sample content",
 *   "Include proper Typst formatting (#set document, #set page, #set text)",
 *   "Export TYPST_TEMPLATES array"
 * ]
 * @deps: []
 * @skills: ["typst", "typescript"]
 */
export const _hole = null;
